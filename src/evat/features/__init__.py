"""Visual feature extraction and temporal feature-sequence construction.

Not the Video Transformer (Phase 6). This layer only converts visual
observations (object crops / full frames) into fixed-dimensional feature
vectors and assembles them into per-track temporal sequences that a future
Transformer would consume.
"""
